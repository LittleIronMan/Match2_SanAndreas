class M2S_Config {
    N = 8; // ширина поля(в тайлах)
    M = 8; // высота поля
    C = 5; // количество цветов
    K = 2; // минимальный размер группы
}
var config = new M2S_Config();
export default config;